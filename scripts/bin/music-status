#!/bin/zsh

#echo ${$(playerctl metadata title)} -  ${$(playerctl metadata artist)}

#playerctl metadata artist && echo " - " && playerctl metadata title
playerctl metadata --format "{{title}} * {{artist}}"

